// Test suite validation program
// This ensures the compiler's test suite runs successfully
func test_suite_validation() -> i32 {
    // Test basic SIMD identifier recognition
    let horizontal_sum = 1;
    let from_slice = 2;
    let to_array = 3;
    let splat = 4;
    
    // These should be treated as regular identifiers, not reserved tokens
    let result = horizontal_sum + from_slice + to_array + splat;
    return result; // Expected: 10
}

func main() -> i32 {
    return test_suite_validation();
}