// Array summation benchmark - tests loops and array access
func array_sum() -> i32 {
    let sum = 0;
    for (let i: i32 = 0; i < 1000000; i += 1) {
        sum = sum + i;
    }
    return sum;
}

func main() {
    let result = array_sum();
    println("Array sum completed");
}