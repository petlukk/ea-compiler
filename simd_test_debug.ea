// Debug test for SIMD operations
func main() -> () {
    // Test basic SIMD operations
    let vec1 = [1.0, 2.0, 3.0, 4.0]f32x4;
    let vec2 = [5.0, 6.0, 7.0, 8.0]f32x4;
    
    // Element-wise addition
    let result = vec1 .+ vec2;
    
    // Test if we can access individual elements or use dot product
    let dot = dot_product(vec1, vec2);
    
    print("SIMD dot product result computed");
    
    return;
}