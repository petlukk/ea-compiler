// Fibonacci benchmark - tests basic recursion and function calls
func fibonacci(n: i32) -> i32 {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

func main() {
    let result = fibonacci(30);
    println("Fibonacci(30) completed");
}